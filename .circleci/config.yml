version: 2
jobs:
  RunMain:
    docker:
      - image: cimg/python:3.5.8

    steps:
    - run: sleep 2s
    - run: python --version
    - run: ls
    - run: sleep 1s
    parallelism: 1





workflows:
  version: 2
  Run-All:
    jobs:
    - RunMain
